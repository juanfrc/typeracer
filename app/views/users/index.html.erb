<h1>All the Users</h1>

<% @users.each do |user| %>
<%= user.name  %>
<%= link_to user.name, user %>
<% end %>
<h1> These are top scores </h1>
<% @top_scores.each do |topscore| %>

<%= topscore[:user_name] %>
<%= topscore[:score] %>
<% end %>